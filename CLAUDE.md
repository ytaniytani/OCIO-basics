# このリポジトリでの進めかた

OCIO と ACES の学習サイトです。仕様は `docs/SPEC.md` にあります。

## 役割を分ける

このリポジトリでは **書く役と評価する役を必ず分けます。**
同じ頭で書いて検査すると、自分の思いこみをそのまま見落とすからです。
実際、書き手が自分で「問題なし」と判断していた原稿から、
評価役があとで数値の誤りを3件見つけています。

| 役 | エージェント | できること | してはいけないこと |
|---|---|---|---|
| **書く役** | `writer` | 本文・部品の作成と修正。ファイルを書き換える | 自分の原稿を自分で合格にする |
| **評価する役** | `reviewer` | 原稿と実装の検査。計算による裏取り | ファイルを書き換える |
| **とりまとめ** | メインの会話 | 依頼、判断、採否、コミット | 章の本文を自分で書き上げる |

### 手順

1. **とりまとめ**が、何をどう直すかを決めて `writer` に渡す。
2. `writer` が書く。書いたら「どこを直したか」を返す。**合否は判定しない。**
3. **とりまとめ**が `reviewer` に検査を依頼する。
   `writer` が何をしたかも伝える(検査範囲を絞るため)。
4. `reviewer` が指摘を返す。**ファイルは触らない。**書き換え文案は出す。
5. **とりまとめ**が指摘の採否を決める。直すものは `writer` に戻す。
6. 指摘が無くなるまで 2〜5 を回す。
7. **とりまとめ**が検証コマンドを走らせ、コミットする。

### 例外

とりまとめが直接ファイルを触ってよいのは次の場合だけです。

- 設定、ビルド道具、CI、エージェント定義そのもの
- 評価役の指摘を受けた1〜2行の機械的な修正
- ユーザーから直接、具体的な修正指示を受けたとき

章の本文をまとめて書く・作り直すときは、必ず `writer` に渡してください。

## 読者

**大人**です。映像・CG・写真の知識はありません。
文系で数学が苦手な人も多く含みます。

**「中学生にもわかる」は難易度の目安であって、読者像ではありません。**
やさしく書きますが、子供扱いはしません。詳しくは `docs/SPEC.md` の「2. 対象読者」。

## 画面

**デスクトップPC(横長の画面)が第一想定**です。スマートフォンは壊れずに読めれば十分。
横幅を使って、図と表が同じ画面に入るようにします。

## 書き換えてよい範囲

本文は各 HTML の `<!-- BODY:START -->` と `<!-- BODY:END -->` の間だけです。
その外側(ヘッダー、目次、フッター)は `tools/build_pages.py` が作ります。
用語集は `docs/GLOSSARY.md` が原本で、`tools/build_glossary.py` が
`glossary.html` と `assets/data/glossary.json` を作ります。
生成物を直接編集しないでください。

## 数字の扱い

**記憶で書かないこと。** 色の数値は必ずその場で計算して確かめます。

```
node --input-type=module -e "
import { TRANSFERS, to8bit, MIDDLE_GREY } from './js/core/color.js';
console.log(TRANSFERS.srgb.encode(0.18), to8bit(TRANSFERS.srgb.encode(0.18)));
"
```

## コミット前に走らせるもの

```
python3 tools/build_pages.py --check
python3 tools/build_glossary.py --check
npx --yes http-server -p 8811 -s . &      # 別プロセスで起動しておく
node tools/check-site.mjs
```

`check-site.mjs` は全ページを 2つの画面幅とJSオフで開き、
外部への通信がゼロであることまで確かめます。
